meta {
  name: ORD API
}

docs {
  Auth, users, and conversations endpoints.

  Session is the HttpOnly `AUTH-TOKEN` cookie set by otp-verify.
  Requests after login use `auth: inherit`.

  See `workflows/conversation-session` for an end-to-end conversation flow.
}
