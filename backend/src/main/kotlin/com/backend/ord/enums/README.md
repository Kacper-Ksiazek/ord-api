# 🗺️ Enum directories structure roadmap:

There are three directories in the **enums** package:

- `persistance` - database custom types.
- `application` - enums for the needs of the application itself.
- `infrastructure` - enums for the needs of the infrastructure, eg. console commands.
